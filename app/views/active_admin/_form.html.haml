- if @effective_asset.new_record?
  = render :partial => 'active_admin/new', :locals => {:asset => @effective_asset}
- else
  = render :partial => 'active_admin/edit', :locals => {:asset => @effective_asset}
